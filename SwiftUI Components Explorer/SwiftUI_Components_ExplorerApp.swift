import SwiftUI

// Main entry point of the SwiftUI application
@main
struct SwiftUI_Components_ExplorerApp: App {
    var body: some Scene {
        // Defines the scene for the app's main window
        WindowGroup {
            ContentView()  // The initial view of the app
        }
    }
}
